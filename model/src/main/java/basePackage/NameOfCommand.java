package basePackage;

/**Enum with all commands (name of commands).*/
public enum NameOfCommand {
    INFO,
    REMOVE,
    ADD,
    ADD_IF_MAX,
    REMOVE_FIRST,
    SHOW,
    REMOVE_GREATER,
    HELP,
    IMPORT,
    EXIT,
    SAVE,
    LOAD;
}
